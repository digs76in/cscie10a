A1.

if ( age >= 22 && age <= 44 && isMale && !isASmoker && height < 72 && weight < 160 && isGoodLooking && isAbleToRelocate) {
  System.out.println("Marry me!");
} else {
  System.out.println("Get Lost!");
}

A2.

x == 0 && y == 0


A3.

while ((i < 10) && (j*j != 25)) {
  i++;
  ++j;
  System.out.println( i * j);
}

A4.

____________________________________________________
| p | q | r | (p && q) || (!r) | !(p && (q || !r)) |
----------------------------------------------------
| f | f | f | t                | t                 |
----------------------------------------------------
| f | f | t | f                | t                 |
----------------------------------------------------
| f | t | f | t                | t                 |
----------------------------------------------------
| t | f | f | t                | f                 |
----------------------------------------------------
| t | t | f | t                | f                 |
----------------------------------------------------
| f | t | t | f                | t                 |
----------------------------------------------------
| t | f | t | f                | t                 |
----------------------------------------------------
| t | t | t | t                | f                 |
----------------------------------------------------

A5.

boolean isJavaIdentifierStart (char ch)
{
    return (ch == 36 || ch == 95 || (ch >= 65 && ch <= 90) || (ch >= 97 && ch <= 122));
}
